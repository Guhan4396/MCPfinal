{
  "buildCommand": "next build",
  "devCommand": "next dev",
  "installCommand": "curl -L https://github.com/conda-forge/miniforge/releases/latest/download/Mambaforge-Linux-x86_64.sh -o miniforge.sh && bash miniforge.sh -b -p $HOME/conda && export PATH=$HOME/conda/bin:$PATH && pip install -r requirements.txt && pnpm install",
  "framework": "nextjs",
  "outputDirectory": ".next",
  "rewrites": [
    {
      "source": "/api/calculate",
      "destination": "/api/calculate"
    }
  ],
  "functions": {
    "app/api/calculate/route.ts": {
      "memory": 1024,
      "maxDuration": 60
    }
  },
  "env": {
    "PYTHON_VERSION": "3.9"
  }
}

